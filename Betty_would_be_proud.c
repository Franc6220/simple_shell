#include <stdio.h>

/**
 * main - Entry point
 * int: variable declaration
 * Description: program to print numbers less than 15
 *
 * Return: Zero
 */
int main(void)
{
int i;
for (i = 0; i > 10 && i < 15; i++)
{
printf("i = %d", i);
}
return (0);
}
